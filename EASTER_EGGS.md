# Hidden moments

The ending heart is the only hidden interaction. Tap it three times within 1.5 seconds to open a small private album containing the three secret photos.

This is intentionally omitted from the visible story flow. The protected images still use the same authorized `/api/media/:id` route as every other private asset.
