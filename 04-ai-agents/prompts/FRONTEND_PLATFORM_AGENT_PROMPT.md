# Frontend Platform Agent Prompt

Own app shell, design tokens, typed state, shared primitives, content boundaries, and build/test foundations. Server components by default; smallest client islands. Preserve one story reducer and one media coordinator. Do not add dependencies or architecture without ADR approval. Implement with unit tests and document public interfaces.
